Task‑Chunked Version (Prevents Token Overload)

-----------------------------------------------------------------------
🔹 CHUNK 1 — Foundation + Auth
-----------------------------------------------------------------------
You are in SELF-HEALING DEVELOPMENT MODE.

You must internally execute:
PLAN → BUILD → REVIEW → FIX → RE-REVIEW → FIX → OPTIMIZE → VALIDATE

Do NOT stop early.

VALIDATION must confirm:
- No TypeScript errors
- No runtime edge cases
- No architectural violations (CLAUDE_PLAN.md)
- No incomplete flows
- No undefined/null risks

CRITICAL CHECKS:
- SSE always ends with "done"
- AI failover is sequential (Groq → Gemini → Nvidia)
- No parallel failover calls
- Currency stored only as EUR
- Cookies are httpOnly
- Zustand state consistent
- API responses match contracts

If ANY issue exists → continue fixing until stable.

Only stop when production-safe.

You are implementing Avolo per CLAUDE_PLAN.md.

Execute FULL workflow (PLAN → BUILD → REVIEW → FIX → OPTIMIZE).

Scope:
- Phase 1: Foundation
- Phase 2: Authentication

Requirements:
- Strict TypeScript
- Tailwind tokens exact
- Prisma correct
- NextAuth v5 with mergeService

After finishing, STOP.
Do NOT proceed to next phases.

-----------------------------------------------------------------------
🔹 CHUNK 2 — Explore Wizard
-----------------------------------------------------------------------
You are in SELF-HEALING DEVELOPMENT MODE.

You must internally execute:
PLAN → BUILD → REVIEW → FIX → RE-REVIEW → FIX → OPTIMIZE → VALIDATE

Do NOT stop early.

VALIDATION must confirm:
- No TypeScript errors
- No runtime edge cases
- No architectural violations (CLAUDE_PLAN.md)
- No incomplete flows
- No undefined/null risks

CRITICAL CHECKS:
- SSE always ends with "done"
- AI failover is sequential (Groq → Gemini → Nvidia)
- No parallel failover calls
- Currency stored only as EUR
- Cookies are httpOnly
- Zustand state consistent
- API responses match contracts

If ANY issue exists → continue fixing until stable.

Only stop when production-safe.

Continue Avolo implementation.

Execute FULL workflow.

Scope:
- Phase 3: Explore Wizard

Focus:
- Typeform UX
- tripStore Zustand correctness
- airport autocomplete + debounce
- DatePicker from spec

STOP after completion.

-----------------------------------------------------------------------
🔹 CHUNK 3 — Search Backend (CRITICAL CHUNK)
-----------------------------------------------------------------------
You are in SELF-HEALING DEVELOPMENT MODE.

You must internally execute:
PLAN → BUILD → REVIEW → FIX → RE-REVIEW → FIX → OPTIMIZE → VALIDATE

Do NOT stop early.

VALIDATION must confirm:
- No TypeScript errors
- No runtime edge cases
- No architectural violations (CLAUDE_PLAN.md)
- No incomplete flows
- No undefined/null risks

CRITICAL CHECKS:
- SSE always ends with "done"
- AI failover is sequential (Groq → Gemini → Nvidia)
- No parallel failover calls
- Currency stored only as EUR
- Cookies are httpOnly
- Zustand state consistent
- API responses match contracts

If ANY issue exists → continue fixing until stable.

Only stop when production-safe.

Continue Avolo implementation.

Execute FULL workflow.

Scope:
- Phase 4: Search Backend

Critical constraints:
- SSE stream must ALWAYS end with "done"
- AI failover must be sequential only
- Providers must normalize to shared schema
- Use Promise.allSettled between categories

STOP after completion.

-----------------------------------------------------------------------
🔹 CHUNK 4 — Results UI
-----------------------------------------------------------------------
You are in SELF-HEALING DEVELOPMENT MODE.

You must internally execute:
PLAN → BUILD → REVIEW → FIX → RE-REVIEW → FIX → OPTIMIZE → VALIDATE

Do NOT stop early.

VALIDATION must confirm:
- No TypeScript errors
- No runtime edge cases
- No architectural violations (CLAUDE_PLAN.md)
- No incomplete flows
- No undefined/null risks

CRITICAL CHECKS:
- SSE always ends with "done"
- AI failover is sequential (Groq → Gemini → Nvidia)
- No parallel failover calls
- Currency stored only as EUR
- Cookies are httpOnly
- Zustand state consistent
- API responses match contracts

If ANY issue exists → continue fixing until stable.

Only stop when production-safe.

Continue Avolo implementation.

Execute FULL workflow.

Scope:
- Phase 5: Results Display

Focus:
- SSE streaming UI
- slot assignment correctness
- filters and pagination

STOP after completion.

-----------------------------------------------------------------------
🔹 CHUNK 5 — Trips
-----------------------------------------------------------------------
You are in SELF-HEALING DEVELOPMENT MODE.

You must internally execute:
PLAN → BUILD → REVIEW → FIX → RE-REVIEW → FIX → OPTIMIZE → VALIDATE

Do NOT stop early.

VALIDATION must confirm:
- No TypeScript errors
- No runtime edge cases
- No architectural violations (CLAUDE_PLAN.md)
- No incomplete flows
- No undefined/null risks

CRITICAL CHECKS:
- SSE always ends with "done"
- AI failover is sequential (Groq → Gemini → Nvidia)
- No parallel failover calls
- Currency stored only as EUR
- Cookies are httpOnly
- Zustand state consistent
- API responses match contracts

If ANY issue exists → continue fixing until stable.

Only stop when production-safe.

Continue Avolo implementation.

Execute FULL workflow.

Scope:
- Phase 6: My Trips

Focus:
- refresh pricing flow
- persisted trips (anon + auth)
- trip detail page

STOP after completion.

-----------------------------------------------------------------------
🔹 CHUNK 6 — Profile
-----------------------------------------------------------------------
You are in SELF-HEALING DEVELOPMENT MODE.

You must internally execute:
PLAN → BUILD → REVIEW → FIX → RE-REVIEW → FIX → OPTIMIZE → VALIDATE

Do NOT stop early.

VALIDATION must confirm:
- No TypeScript errors
- No runtime edge cases
- No architectural violations (CLAUDE_PLAN.md)
- No incomplete flows
- No undefined/null risks

CRITICAL CHECKS:
- SSE always ends with "done"
- AI failover is sequential (Groq → Gemini → Nvidia)
- No parallel failover calls
- Currency stored only as EUR
- Cookies are httpOnly
- Zustand state consistent
- API responses match contracts

If ANY issue exists → continue fixing until stable.

Only stop when production-safe.

Continue Avolo implementation.

Execute FULL workflow.

Scope:
- Phase 7: Profile & Preferences

Focus:
- autosave debounce
- currency switching
- GDPR export/delete

STOP after completion.

-----------------------------------------------------------------------
🔹 CHUNK 7 — Journal
-----------------------------------------------------------------------
You are in SELF-HEALING DEVELOPMENT MODE.

You must internally execute:
PLAN → BUILD → REVIEW → FIX → RE-REVIEW → FIX → OPTIMIZE → VALIDATE

Do NOT stop early.

VALIDATION must confirm:
- No TypeScript errors
- No runtime edge cases
- No architectural violations (CLAUDE_PLAN.md)
- No incomplete flows
- No undefined/null risks

CRITICAL CHECKS:
- SSE always ends with "done"
- AI failover is sequential (Groq → Gemini → Nvidia)
- No parallel failover calls
- Currency stored only as EUR
- Cookies are httpOnly
- Zustand state consistent
- API responses match contracts

If ANY issue exists → continue fixing until stable.

Only stop when production-safe.

Continue Avolo implementation.

Execute FULL workflow.

Scope:
- Phase 8: Travel Journal

Focus:
- AI generation
- markdown rendering
- caching

STOP after completion.

-----------------------------------------------------------------------
🔹 CHUNK 8 — Admin
-----------------------------------------------------------------------
You are in SELF-HEALING DEVELOPMENT MODE.

You must internally execute:
PLAN → BUILD → REVIEW → FIX → RE-REVIEW → FIX → OPTIMIZE → VALIDATE

Do NOT stop early.

VALIDATION must confirm:
- No TypeScript errors
- No runtime edge cases
- No architectural violations (CLAUDE_PLAN.md)
- No incomplete flows
- No undefined/null risks

CRITICAL CHECKS:
- SSE always ends with "done"
- AI failover is sequential (Groq → Gemini → Nvidia)
- No parallel failover calls
- Currency stored only as EUR
- Cookies are httpOnly
- Zustand state consistent
- API responses match contracts

If ANY issue exists → continue fixing until stable.

Only stop when production-safe.

Continue Avolo implementation.

Execute FULL workflow.

Scope:
- Phase 9: Admin Dashboard

Focus:
- metrics
- role protection
- aggregation queries

STOP after completion.

-----------------------------------------------------------------------
🔹 CHUNK 9 — Deployment
-----------------------------------------------------------------------
You are in SELF-HEALING DEVELOPMENT MODE.

You must internally execute:
PLAN → BUILD → REVIEW → FIX → RE-REVIEW → FIX → OPTIMIZE → VALIDATE

Do NOT stop early.

VALIDATION must confirm:
- No TypeScript errors
- No runtime edge cases
- No architectural violations (CLAUDE_PLAN.md)
- No incomplete flows
- No undefined/null risks

CRITICAL CHECKS:
- SSE always ends with "done"
- AI failover is sequential (Groq → Gemini → Nvidia)
- No parallel failover calls
- Currency stored only as EUR
- Cookies are httpOnly
- Zustand state consistent
- API responses match contracts

If ANY issue exists → continue fixing until stable.

Only stop when production-safe.

Continue Avolo implementation.

Execute FULL workflow.

Scope:
- Phase 10: Deployment

Focus:
- standalone build
- env correctness
- Prisma migrate
- Plesk configuration

STOP after completion.